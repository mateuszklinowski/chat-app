export enum Theme {
    Light = 'light',
    Dark = 'dark',
}

export enum Lang {
    En = 'en',
    Pl = 'pl',
}

export enum ClockDisplay {
    Hours12 = '12hours',
    Hours24 = '24hours',
}

export enum CtrlEnter {
    On = 'on',
    Off = 'off',
}

export enum Path {
    Home = '/',
    Settings = '/settings',
}
